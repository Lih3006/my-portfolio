import styled from 'styled-components';

export const StyledTagText = styled.p`
  font-size: 0.8rem;
  font-family: 'Lexend';
  border-radius: 1rem;
  font-weight: 600;
  display: inline;
  background-color: rgba(216, 30, 91, 0.2);
  color: #ff6f9f;
  padding: 0.575rem;
  margin: 0.5rem 0.5rem 0.5rem 0;
`;
